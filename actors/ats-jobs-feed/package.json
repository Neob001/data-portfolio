{
  "name": "ats-jobs-feed",
  "version": "0.1.0",
  "type": "module",
  "description": "Searchable, deduplicated feed of job openings from public ATS job-board APIs (Greenhouse, Lever, Ashby, Workable, Recruitee).",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "apify": "^3.2.6"
  },
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test 'tests/*.test.mjs'"
  }
}
