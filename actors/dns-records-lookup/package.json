{
  "name": "dns-records-lookup",
  "version": "0.1.0",
  "type": "module",
  "description": "Bulk DNS lookups for any list of domains: A, AAAA, MX, TXT, NS, CNAME, SOA, CAA plus mail/DNS provider detection, SPF/DMARC presence and TXT verification tokens.",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "apify": "^3.2.6"
  },
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test 'tests/*.test.mjs'"
  }
}
