{
  "name": "sitemap-url-extractor",
  "version": "0.1.0",
  "type": "module",
  "description": "Extract every URL from a website's sitemaps (robots.txt discovery, sitemap indexes, lastmod/priority) and optionally che",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "apify": "^3.2.6"
  },
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test 'tests/*.test.mjs'"
  }
}
